import { Timeout.InterceptorInterceptor } from './timeout.interceptor.interceptor';

describe('Timeout.InterceptorInterceptor', () => {
  it('should be defined', () => {
    expect(new Timeout.InterceptorInterceptor()).toBeDefined();
  });
});
